---
title: "@triggery/redux"
description: "Адаптер Redux для Triggery — читай состояние стора из условия триггера, не подписывая компонент на ререндеры."
sidebar:
  order: 8
  badge:
    text: Tier 2
    variant: note
---

import { Aside, LinkCard, CardGrid, TabItem } from '@astrojs/starlight/components';

import PackageManagers from '~/components/PackageManagers.astro';

Читай [Redux](https://redux.js.org)-стор из условия Triggery без подписки компонента на ререндеры. Работает с обычным Redux и `@reduxjs/toolkit` — хук вызывает `store.getState()` только в момент срабатывания.

[![npm](https://img.shields.io/npm/v/@triggery/redux?style=flat-square)](https://www.npmjs.com/package/@triggery/redux)
[![bundle](https://img.shields.io/bundlephobia/minzip/@triggery/redux?style=flat-square&label=gzip)](https://bundlephobia.com/package/@triggery/redux)

## Установка

<PackageManagers pkg="@triggery/core @triggery/react @triggery/redux redux" />

Peer-зависимости: `react >= 18.0.0`, `redux ^4 || ^5`. `@reduxjs/toolkit` реэкспортирует ту же форму `Store` и работает из коробки.

## Что внутри

| Экспорт | Назначение |
|---|---|
| `useReduxCondition(trigger, name, store, selector)` | Регистрирует условие, читающее Redux. Компонент **не** подписывается на стор. |

## Быстрый пример

```tsx
import { configureStore } from '@reduxjs/toolkit';
import { createTrigger } from '@triggery/core';
import { useReduxCondition } from '@triggery/redux';
import { rootReducer } from './rootReducer';

const store = configureStore({ reducer: rootReducer });

type State = ReturnType<typeof store.getState>;
type Settings = State['settings'];

const messageTrigger = createTrigger<{
  events: { 'new-message': { text: string } };
  conditions: { settings: Settings };
  actions: { showToast: { body: string } };
}>({
  id: 'message-received',
  events: ['new-message'],
  required: ['settings'],
  handler({ event, conditions, actions }) {
    if (!conditions.settings.notifications) return;
    actions.showToast?.({ body: event.payload.text });
  },
});

function SettingsBridge() {
  useReduxCondition(messageTrigger, 'settings', store, (s) => s.settings);
  return null;
}
```

## Как это работает

Triggery — pull-only: селектор вызывается **только** при срабатывании триггера, а не на каждый dispatch. Хук **не** подписывает компонент на стор — диспатчи никогда не ререндерят bridge-компонент. Если отдельному компоненту нужен тот же срез в JSX — рядом используй `useSelector` из `react-redux`.

## API

```ts
useReduxCondition<T, S, K>(
  trigger: Trigger<S>,
  name: K,
  store: { getState(): T },
  selector: (state: T) => ConditionMap<S>[K],
): void
```

## Связанные пакеты

<CardGrid>
  <LinkCard title="@triggery/core" description="Экспортирует Trigger<S>." href="/ru/packages/core/" />
  <LinkCard title="@triggery/react" description="useReduxCondition — это React-хук." href="/ru/packages/react/" />
  <LinkCard title="@triggery/zustand" description="Альтернативный адаптер для Zustand-сторов." href="/ru/packages/zustand/" />
  <LinkCard title="@triggery/jotai" description="Альтернативный адаптер для Jotai-атомов." href="/ru/packages/jotai/" />
  <LinkCard title="@triggery/codemod" description="migrate-from-listener-middleware переносит эффекты RTK в триггеры." href="/ru/packages/codemod/" />
</CardGrid>

## Исходники

- [GitHub](https://github.com/triggeryjs/triggery/tree/main/packages/redux)
- [npm](https://www.npmjs.com/package/@triggery/redux)
- [Changelog](https://github.com/triggeryjs/triggery/blob/main/packages/redux/CHANGELOG.md)
