---
title: "@triggery/reatom"
description: "Адаптер Reatom для Triggery — читай значение атома из условия триггера без подписки хост-компонента."
sidebar:
  order: 11
  badge:
    text: Tier 2
    variant: note
---

import { Aside, LinkCard, CardGrid, TabItem } from '@astrojs/starlight/components';

import PackageManagers from '~/components/PackageManagers.astro';

Читай атом или computed из [Reatom](https://www.reatom.dev) в условии Triggery. Pull-only: атом вызывается только при срабатывании триггера, ни один подписчик к нему не привязан.

[![npm](https://img.shields.io/npm/v/@triggery/reatom?style=flat-square)](https://www.npmjs.com/package/@triggery/reatom)
[![bundle](https://img.shields.io/bundlephobia/minzip/@triggery/reatom?style=flat-square&label=gzip)](https://bundlephobia.com/package/@triggery/reatom)

## Установка

<PackageManagers pkg="@triggery/core @triggery/react @triggery/reatom @reatom/core" />

Peer-зависимости: `react >= 18.0.0`, `@reatom/core ^1000 || ^1001`.

## Что внутри

| Экспорт | Назначение |
|---|---|
| `useReatomCondition(trigger, name, atom, selector?)` | Регистрирует условие на основе Reatom. Работает с любым вызываемым примитивом Reatom — атомами, computed, производными значениями. |

## Быстрый пример

```tsx
import { atom } from '@reatom/core';
import { createTrigger } from '@triggery/core';
import { useReatomCondition } from '@triggery/reatom';

type Settings = { sound: boolean; notifications: boolean };

const $settings = atom<Settings>({ sound: true, notifications: true });

const messageTrigger = createTrigger<{
  events: { 'new-message': { text: string } };
  conditions: { settings: Settings };
  actions: { showToast: { body: string } };
}>({
  id: 'message-received',
  events: ['new-message'],
  required: ['settings'],
  handler({ event, conditions, actions }) {
    if (!conditions.settings.notifications) return;
    actions.showToast?.({ body: event.payload.text });
  },
});

function SettingsBridge() {
  useReatomCondition(messageTrigger, 'settings', $settings);
  return null;
}
```

С селектором-проекцией:

```ts
useReatomCondition(messageTrigger, 'settings', $profile, (p) => p.settings);
```

## Как это работает

Pull-only: атом вызывается (`atom()`) **только** при срабатывании триггера. Подписчик не регистрируется, поэтому хост-компонент никогда не ререндерится на обновлениях атома. Для компонентов, которым нужно рендерить значение, используй собственные React-биндинги Reatom (`reatomComponent`, `useAtom`).

## API

```ts
useReatomCondition<V, S, K>(
  trigger: Trigger<S>,
  name: K,
  atom: () => V,
  selector?: (value: V) => ConditionMap<S>[K],
): void
```

## Связанные пакеты

<CardGrid>
  <LinkCard title="@triggery/core" description="Экспортирует Trigger<S>." href="/ru/packages/core/" />
  <LinkCard title="@triggery/react" description="useReatomCondition — это React-хук." href="/ru/packages/react/" />
  <LinkCard title="@triggery/jotai" description="Альтернативный адаптер для состояния в форме атомов." href="/ru/packages/jotai/" />
  <LinkCard title="@triggery/signals" description="Альтернативный адаптер для состояния в форме сигналов." href="/ru/packages/signals/" />
  <LinkCard title="@triggery/zustand" description="Адаптер для состояния в форме стора." href="/ru/packages/zustand/" />
</CardGrid>

## Исходники

- [GitHub](https://github.com/triggeryjs/triggery/tree/main/packages/reatom)
- [npm](https://www.npmjs.com/package/@triggery/reatom)
- [Changelog](https://github.com/triggeryjs/triggery/blob/main/packages/reatom/CHANGELOG.md)
