---
title: API Reference
description: "Every public symbol across every `@triggery/*` package — a tree with a per-kind icon."
---

import { Aside, LinkCard, CardGrid } from '@astrojs/starlight/components';
import SymbolTree from '~/components/SymbolTree.astro';
import { sections } from '~/data/api-symbols.ts';

A complete index of public exports, grouped by package. The icon on the left of each name shows the kind of symbol: <span class="legend-fn">ƒ</span> function, <span class="legend-comp">⌘</span> component, <span class="legend-type">T</span> TypeScript type, <span class="legend-const">∙</span> constant. The link icon on the right opens the page with full signature and examples.

<Aside type="tip" title="Where to look when there is no per-symbol page">
Most adapters and tooling helpers do not have a dedicated page in this reference yet — their narrative documentation lives in [the Packages section](/packages/).
</Aside>

{sections.map((s) => (
  <section>
    <h2 id={s.id}>{s.title}</h2>
    <SymbolTree packages={s.packages} />
  </section>
))}

## See also

<CardGrid>
  <LinkCard title="Packages overview" description="Every @triggery/* package with a narrative page and quick start." href="/packages/" />
  <LinkCard title="Guide" description="Concepts, patterns, recipes — the narrative documentation that surrounds this reference." href="/guide/" />
  <LinkCard title="Ecosystem" description="Adapters, integrations, templates, the Awesome page." href="/ecosystem/" />
</CardGrid>
