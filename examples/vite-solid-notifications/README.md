# Triggery — notifications (Vite + Solid)

Same trigger as the React notifications example, Solid components. Demonstrates that the trigger file is framework-agnostic.

```bash
pnpm install
pnpm dev
```
