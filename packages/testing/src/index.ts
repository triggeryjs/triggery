/**
 * @triggery/testing — testing utilities for Triggery.
 *
 * V1 stub. The full API (`createTestRuntime`, `mockCondition`, `mockAction`, `fakeScheduler`)
 * lands in Phase 2 (DX scaffolding).
 */

export const PLACEHOLDER_VERSION = '0.0.0';
